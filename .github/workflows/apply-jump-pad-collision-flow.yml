name: Apply jump-pad collision flow

on:
  issues:
    types: [opened]

permissions:
  contents: write

jobs:
  apply:
    if: github.event.issue.title == '__apply_jump_pad_collision_flow_20260721__'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Rewrite jump-pad lesson flow
        shell: python
        run